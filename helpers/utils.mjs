// export const functionAsync = (functionToAsked, params) => {
//   return new Promise((resolve, reject) => {
//     functionToAsked(params, (error, success) => {
//       if (error) {
//         reject(error);
//       } else {
//         resolve(success);
//       }
//     });
//   });
// }

// export default const ok = (params, )
